import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function CaptureScreen() {
  return (
    <PlaceholderScreen
      title="Capture"
      body="Voice + camera will land here. Photos and audio upload to private Storage; captures start Unassigned."
    />
  );
}
