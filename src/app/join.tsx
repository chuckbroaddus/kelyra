import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function JoinScreen() {
  return (
    <PlaceholderScreen
      title="Join class"
      body="Student class-code + pick roster name. To-do only: focus skill and assigned practice."
    />
  );
}
