import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function InboxScreen() {
  return (
    <PlaceholderScreen
      title="Inbox"
      body="Unassigned captures and drafts waiting for Approve. Matcher never invents a student."
    />
  );
}
