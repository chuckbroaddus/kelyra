import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function ParentScreen() {
  return (
    <PlaceholderScreen
      title="Parent progress"
      body="Invite link bound to one student. Shows focus skill, assigned/done, and the approved one-liner. No scores or photos."
    />
  );
}
