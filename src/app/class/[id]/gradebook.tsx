import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function GradebookScreen() {
  return (
    <PlaceholderScreen
      title="Grade book"
      body="Teacher-only grid: captured work + assigned practice. Draft until Approve. No weights or CSV in MVP."
    />
  );
}
