import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function AssignScreen() {
  return (
    <PlaceholderScreen
      title="Assign practice"
      body="From an approved gap, generate 3–8 items and assign to that student. Implementation is a server Edge Function."
    />
  );
}
