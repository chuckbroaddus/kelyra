/**
 * Soft working mark (native) — shared RN View tree in SoftMarkShared.
 * No WebView / no HTML Soft host at runtime.
 */
export {
  SoftMark,
  ORBIT_PAD_FRAC,
  type SoftMode,
} from '@/components/ui/SoftMarkShared';
