/**
 * Soft web — same native RN View tree as SoftMarkShared (no WebView / no HTML host).
 * HTML Soft v8b remains design SoT only.
 */
export {
  SoftMark,
  ORBIT_PAD_FRAC,
  type SoftMode,
} from '@/components/ui/SoftMarkShared';
