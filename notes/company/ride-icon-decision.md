# RIDE-ICON-P1 — Locked Ride chrome glyph

**Date:** 2026-09-09  
**Author:** product-manager (card t_c1c9f3cd)  
**Status:** LOCKED — Engineering implements via `kelyra-qa-loop`  
**Sources:** `notes/company/ride-icon-research.md`, `notes/company/ride-icon-options.md`, `notes/company/ride-icon-options.html`

## LOCKED

| Field | Value |
|-------|-------|
| Option | **A — RearPlate** |
| Pack id | `rideRear` (designer pack name; recipe key below is product name) |
| **IconName** | **`ride`** |
| Rationale | CEO wants dead-rear car + small plate; designer non-binding default is A; they agree — no fifth option. Canonical chrome name is short `ride` (matches tray key `ride`, titles “Ride” / car-rider). |

## Decision rationale (short)

1. Current `work` on Parent Ride tray and Office “Dismissal curb” reads as paper/assignments — wrong job.
2. Job is photograph/enter plate on the car ahead (LPR), not placard paper.
3. A is the only pack that is both CEO-direction and designer default; B/C/D add depth, plate-only, or bumper tradeoffs without CEO pull.
4. IconName `ride` not `rideRear`: one product glyph for Ride chrome; pack id stays historical only.

## Out of scope (do not touch)

- Student Assignments / Class desk `work` icon (`src/lib/chrome/classTabs.ts` Assignments stays `work`).
- Ride office Manage row (`icon="manage"` on “Ride office”) — already correct.
- New metaphors, View-stroke glyphs, redesign of recipe geometry.
- SQL, Edge, git commit/push (unless CEO already authorized separately for implement step).

## Surfaces IN scope

| Surface | File | Change |
|---------|------|--------|
| Parent floating tray Ride tab | `src/lib/chrome/trayTabs.ts` | `icon: 'work'` → `icon: 'ride'` on parent `{ key: 'ride', ... }` only |
| Office Manage “Dismissal curb” | `src/app/index.tsx` | ListRow `icon="work"` → `icon="ride"` |

## Icon system steps (Engineering)

1. Add recipe `ride` to `RECIPES` in `scripts/build-icons.mjs` (copy block below; do not redesign).
2. Add `'ride'` to `IconName` union in `src/components/ui/Icon.tsx` (web twin uses string + `ICON_ASSETS`; no separate union required if unchanged).
3. Run `npm run icons` → regenerates `assets/icons/ride.png` + `src/components/ui/iconAssets.ts`.
4. Swap the two call sites above.
5. Visual QA at 22–24 px light/dark tint; plate ticks must remain legible.

## LOCKED recipe (copy verbatim into RECIPES)

```js
ride: (p) => {
  // roof (rear glass / roof cap)
  roundRect(p, 7.0, 3.4, 10.0, 3.2, 1.4, ST, false);
  // body / trunk face
  roundRect(p, 4.6, 5.8, 14.8, 11.0, 1.6, ST, false);
  // taillights (filled dots)
  circle(p, 6.2, 8.6, 0.95, 0, true);
  circle(p, 17.8, 8.6, 0.95, 0, true);
  // license plate (must read at 22px)
  roundRect(p, 8.0, 10.6, 8.0, 3.6, 0.7, ST, false);
  line(p, 9.2, 12.4, 10.6, 12.4, ST);
  line(p, 11.3, 12.4, 12.7, 12.4, ST);
  line(p, 13.4, 12.4, 14.8, 12.4, ST);
  // wheels
  circle(p, 7.4, 18.6, 2.05, ST, false);
  circle(p, 16.6, 18.6, 2.05, ST, false);
},
```

**Recipe notes (from pack; do not redesign):** ST=1.9; roof height ≥ 2.8; three plate ticks only; wheel–plate gap ≥ 1.5; white-ink only.

## File list (implement PR)

| Path | Action |
|------|--------|
| `scripts/build-icons.mjs` | Add `ride` recipe |
| `src/components/ui/Icon.tsx` | Extend `IconName` with `'ride'` |
| `src/components/ui/iconAssets.ts` | Regenerated by `npm run icons` |
| `assets/icons/ride.png` | Generated by `npm run icons` |
| `src/lib/chrome/trayTabs.ts` | Parent Ride tab icon → `ride` |
| `src/app/index.tsx` | Dismissal curb ListRow icon → `ride` |
| Tests (below) | Assert icon names / parent-only Ride |

## Tests / acceptance criteria

Engineering can ship when **all** are true:

1. **Icon pipeline:** `ride` exists in `RECIPES`, `IconName`, `ICON_ASSETS`, and `assets/icons/ride.png` after `npm run icons`.
2. **Tray parent-only Ride unchanged:** existing tests still pass — parent tray keys include `ride`; teacher/admin do **not** get Ride tab (`trayTabs.test.ts`, `altitudeLocks.security.test.ts` patterns).
3. **Icon swap (cheap static asserts OK):**
   - Parent Ride tab config uses `icon: 'ride'` (or equivalent) in `trayTabs.ts`.
   - `src/app/index.tsx` Dismissal curb ListRow uses `icon="ride"` (or `icon={'ride'}`).
   - Class Assignments still `icon: 'work'` in `classTabs.ts` — **must not** retarget student/class `work`.
4. **No other `work` → `ride` bulk replace.** Grep: only the two in-scope surfaces change for Ride chrome; Assignments/`work` paper glyph remains for homework.
5. **Typecheck:** `npm run typecheck` clean for IconName usage.
6. **Visual:** at tray size, glyph reads as car rear + plate, not paper, not camera body (`photo`/`capture`).

## Explicit non-goals for implementer

- Do not implement B/C/D.
- Do not keep pack key `rideRear` as IconName unless blocked by a collision (there is none today).
- Do not change labels, hrefs, or Ride product copy in this PR.
- Do not touch `notes/teacher-decks/`.

## Rejected options (record)

| Option | Why not locked |
|--------|----------------|
| B `rideQuarter` | Depth may echo `photo`; CEO asked dead-rear, not ¾. |
| C `ridePlate` | Strong LPR but brackets risk focus/capture; weaker “car line” tray read. |
| D `rideBumper` | Curb-minimal; bumper can read as bar; less full vehicle than CEO ask. |

## Recommended next action

Engineering: implement via `kelyra-qa-loop` using this note as the full request. CEO already authorized implement + commit + merge + push after this pick (per card body) — still run qa-loop; do not self-certify outside loop.

**Epic:** t_c96c0695 · **This card:** t_c1c9f3cd · **Designer pack:** t_5b967ed5
