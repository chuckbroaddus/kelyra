# Kelyra PM weekly — leadership sync input

**Date:** 2026-09-08 CT  
**Author:** product-manager  
**Board SoT:** Hermes `kelyra` (blocked=8, ready=0, todo=0, done=189)  
**Desk snapshot:** `notes/dashboard/worklist.json` (stale 2026-09-03; prefer kanban)

---

## OBJECTIVE

Rank this week’s product bets vs Chuck-locked HOLDs. Spec-only P0 acceptance where recommended. No app code. Do not unstick locked HOLDs.

## CONTEXT

### Board (8 blocked HOLDs — leave parked)

| ID | Title | Why locked |
|----|-------|------------|
| `t_7d367b42` | Q18 lesson-assign acceptance | HOLD test plan, not Build; parked post-migration |
| `t_908912d4` | School calendar | Epic tracker; research/plan pack already exists under `notes/company/calendar-*.md` |
| `t_4d69845a` | Accreditation lesson plans | Epic tracker; pack exists `lesson-plan-*.md` |
| `t_5a34b1ca` | Class landing page | Epic tracker; pack exists `class-landing-*.md` |
| `t_687d9499` | License-clean assignment fixtures | Epic tracker; fixtures already under `notes/qa-fixtures/` |
| `t_15ae546e` | Photo + answer-key grading | Epic tracker; pack exists `photo-key-grading-*.md` |
| `t_7fb77277` | PPT-to-practice | CEO HOLD; `/assign` stub by design |
| `t_7b12ad6f` | Attendance | Out of MVP until CEO sends |

### Shipped / landed (recent)

- **GAUTH / LaTeX / MATHUI** — Explain/Help/Ask math + prose renderer track done (multiple cards; KaTeX native path).
- **RIDE v1** — car-rider photo/LPR track GATE-sent and done (`t_b11cfea2` / related); phone LPR, 7-day TTL on photos+events, duty wall (not `is_staff`).
- **F04 / T08** — done as **accept-only** (also_administrator school-wide via `can_message` / full profile rows to allowed peers: product-accepted residual, not this week’s build).
- **Diary + My Ledger v1** — done (`t_f9b1198c`).
- **SchoolMarm chrome/feeds/discrepancy** — done (`t_e58a997e`); **ingest** remains Grok Bot 8:00 / 18:30 CT (ops, not class-app loop).
- **TEACH-UX A–D + leftovers L** — cards done; CEO bar still: teacher chrome denser than Super/Student (see residual risk below).
- **Mobile pre-auth portrait lock (T40)** — landed native-only; web splash layout/audio still a known scrub (`notes/dashboard/qa-web-splash/report.md`).

### Product law (unchanged)

Phone captures; web reviews/assigns/grades. Matcher never inserts students. Nothing is a grade until Approve. Teachers do not create classes. Model keys server-side. FERPA walls over chrome hides.

---

## REQUIREMENTS (this week’s ranked bets)

### Tier 0 — Do not touch (HOLD)

1. PPT-to-practice (`t_7fb77277`)
2. Attendance (`t_7b12ad6f`)
3. Epic parent cards CAL / LPLAN / LAND / QA-FIX / KEYGRADE (implementation not authorized; research packs already exist)
4. Q18 as Build send (it is acceptance-only; do not treat as implement)

### Tier 1 — P0 this week (recommend)

**P0-A — Teacher chrome clarity residual (dogfood bar)**  
Problem: Super/Student feel single-purpose; teacher still “many apps in one chrome” even after TEACH-UX ship.  
Bet: **CEO dogfood pass + punch list only** — no new IA rewrite. Close any residual dual-hat / Needs / demoted-tab discoverability that still fails the Super/Student clarity bar. Spec: `notes/company/teacher-ux-plan.md` + acceptance `teacher-ux-acceptance.md`.

**P0-B — Pre-auth splash / login polish (web + portrait mobile confirmation)**  
Problem: Mobile portrait lock landed; web still has centering/cover/audio debt; brand first impression.  
Bet: one focused loop for **US-WEB-1 + US-WEB-2 + US-WEB-3** from `notes/dashboard/qa-web-splash/report.md`. Confirm native portrait pre-auth still holds after sign-in unlock.

**P0-C — Q18 lesson-assign acceptance run (QA only, not Build)**  
Problem: L1/L4 lesson path shipped; acceptance plan still blocked as card but product needs live evidence.  
Bet: **human/QA execute** `notes/qa-lesson-assignment-acceptance.md` / card `t_7d367b42` as a *test run*, file bugs — **do not unblock as implement**. CoS may leave card blocked and spawn a separate QA-run card if desired.

### Tier 2 — P1 this week if capacity (after P0)

| Rank | Bet | Rationale | Not |
|------|-----|-----------|-----|
| P1-1 | **KEYGRADE method review → thin slice decision** | Core MVP loop (photo → gap → Approve). Pack complete; CEO decide pipeline only | No full implement without send |
| P1-2 | **Calendar v1 scope freeze** | Spec pack complete; freeze *due dates + class events + publish gate* only | No sports mega-calendar, no twin-mix |
| P1-3 | **License fixtures QA wiring** | Fixtures exist; point QA harness at `notes/qa-fixtures/` | No content business |
| P1-4 | **Class landing / lesson-plan** | Spec-ready; lower than core loop | No implement until CEO send |
| P1-5 | F05/T11 dead family-link chrome (`isAdminRole`) | UX fail-closed already; hide dead controls | Not F04 reopen |

### Tier 3 — Park / later

- A2 safe-read Ask tools (HOLD unless CoS sends)
- U3 office password reset (HOLD)
- AVG syllabus family transparency (spec exists; not this week)
- Static JWT test upgrades (P3 burn window only)
- SchoolMarm ingest: leave on Grok Bot schedule; class app does not own

---

## P0 acceptance criteria (spec only)

### P0-A — Teacher chrome residual

**User stories**

1. As a pure teacher, I see only class-desk chrome (Desk · Capture · Needs · Class · Ask). No Office People / Manage / Matrix / school-wide Activity as primary chrome.
2. As dual-hat teacher+admin on **Teach seat**, Needs badge and tray match pure teacher; Office seat has zero Needs badge and office tray.
3. As a teacher, Needs badge count equals what `/inbox` lists (or a documented thinner consistent set).
4. As a teacher, Week and Heatmap remain reachable without returning as default ClassTabs icons.
5. As CEO dogfood, teacher home answers “what do I do now?” in ≤2 taps for capture, needs review, and roster — without office nouns.

**Acceptance (must all pass)**

| # | Criterion |
|---|-----------|
| A1 | Pure teacher: zero Office People / Manage / Matrix / school Activity primary entries (drawer + tray + home). |
| A2 | Dual-hat Teach seat: `needsCount` === pure teacher path; Office seat needsCount = 0. |
| A3 | Badge count ↔ `/inbox` contents agree (same filter definition in one place). |
| A4 | CLASS_TABS default ≤7; week/heatmap not default icons; deep-link still works and highlights nearby tab. |
| A5 | Teachers still cannot create classes (no Create control; RPC fail-closed). |
| A6 | No new tray tab; no `/needs` route rename; no student-skin chrome. |
| A7 | Matcher never inserts students; nothing is a grade until Approve (regression). |
| A8 | CEO or CoS dogfood sign-off: “clearer than pre-TEACH-UX; not worse than Super/Student bar.” |

**Out of scope:** ClassTabs recut rewrite, seat SQL table, F04/T08 reopen, PPT/Attendance.

---

### P0-B — Splash / login (web + mobile confirm)

**User stories**

1. As a desktop visitor, Sign in + fields are centered over full-bleed splash.
2. As a visitor, splash video/still cover the content viewport (no left strip, no tiny logo on black).
3. As a web visitor, I can hear splash audio via a non-skip gesture when autoplay is blocked.
4. As a phone user pre-auth, UI stays portrait; post sign-in orientation unlocks.

**Acceptance**

| # | Criterion |
|---|-----------|
| B1 | Web ≥360px: auth column horizontally centered (`alignSelf: 'center'` or equiv); maxWidth ≤360. |
| B2 | Web 1280×800 and 390×844: media full-bleed cover; resize does not leave permanent black gutters. |
| B3 | Crossfade architecture preserved (still under video; no full black hold). |
| B4 | Web audio: muted-start OK; dedicated or non-skip gesture unmutes; skip still available. |
| B5 | Native phone pre-auth portrait lock still on; unlock after authenticated session. |
| B6 | No tray/header on signed-out splash; no `EXPO_PUBLIC_` keys; no remote splash fetch. |
| B7 | Focused tests for centering/cover references + portrait lock behavior where assertable. |

**Out of scope:** expo-av full migration (optional P2), brand redesign, auth/RLS changes.

---

### P0-C — Q18 lesson-assign acceptance **run** (QA)

**Not a Build send.** Execute existing plan.

| # | Criterion |
|---|-----------|
| C1 | Teacher A assigns published lesson to Class A / S1 without Create class. |
| C2 | Student Open plays in-app WebView; identity is that student only (S2 never sees S1). |
| C3 | Complete writes results onto student record; Grade/Class/Student altitudes can see evidence (not auto-grade). |
| C4 | Teacher B cannot open Class A lesson results; parent sees only own child progress rules. |
| C5 | Bugs filed with repro / expected / actual / severity; no silent “pass” without evidence. |
| C6 | Card `t_7d367b42` stays blocked as implement; optional separate QA-run card for the execution. |

---

## CONSTRAINTS

- No application code from this PM pass.
- Do **not** unblock Chuck-locked HOLDs (PPT, Attendance, epic parents, Q18-as-Build).
- Do not invent product past tightened MVP (`docs/mvp.md`).
- SuperGrok ~12% weekly/Chicago day via ARM; CoS GRANT before ready work that burns pools.
- SchoolMarm ingest: Grok Bot 8am/6:30pm CT — not class-app P0.
- F04/T08 remain accept-only; do not reopen as P0.

## FILES/AREAS

- Board: Hermes `kelyra`
- Desk: `notes/dashboard/worklist.json` (legacy)
- This handoff: `notes/company/PM_WEEKLY_2026-09-08.md`
- Teacher UX: `notes/company/teacher-ux-*.md`, `teach-ux-leftovers-request.md`
- Splash: `notes/dashboard/qa-web-splash/report.md`
- Lesson QA: `notes/qa-lesson-assignment-acceptance.md`
- Spec packs (HOLD implement): `calendar-*`, `lesson-plan-*`, `class-landing-*`, `photo-key-grading-*`, `notes/qa-fixtures/`
- RIDE pack: `car-rider-*`, `ride-impl-request.md`
- MVP: `docs/mvp.md`

## WORK PERFORMED

1. Read map, operating principles, worklist, grok-build-queue, MVP.
2. Live `hermes kanban --board kelyra stats` + full blocked show (8 HOLDs).
3. Confirmed research/plan packs already on disk for calendar, lesson-plan, class-landing, key-grade, fixtures, RIDE, TEACH-UX, GAUTH/MATHUI.
4. Ranked week bets; wrote P0-A/B/C acceptance; parked HOLD list.

## VERIFICATION

- Board counts: blocked 8, ready 0, todo 0, done 189 (2026-09-08 ~11:10 CT).
- Blocked IDs match CEO context list (Q18, CAL, LPLAN, LAND, fixtures, KEYGRADE, PPT, ATTEND).
- No HOLD unblocked. No code changes. No SQL. No git commit/push.

## RESULT

Weekly PM input artifact at:

`/Users/chuckbroaddus/projects/kelyra/notes/company/PM_WEEKLY_2026-09-08.md`

**This week P0 bets (in order):** Teacher chrome residual dogfood → Splash/login web polish + mobile portrait confirm → Q18 acceptance **run** (QA evidence only).

**Explicit non-bets:** PPT, Attendance, epic implementations, F04/T08 reopen, A2 Ask tools, U3 password reset.

## OPEN ISSUES

1. `worklist.json` `updatedAt` 2026-09-03 — stale vs kanban; do not treat OPEN F* rows as live dispatch.
2. Epic parent cards still blocked while child research/plan is done — CoS should mark epic “spec complete / implement HOLD” so board age warnings don’t look like missing research.
3. Fixtures epic blocked though `notes/qa-fixtures/` exists — close or re-scope epic vs QA wiring only.
4. Teacher clarity bar is qualitative — needs CEO dogfood, not only green tests.
5. Web splash may still need US-WEB-6/7 (HTML5 cover + repeatable audio) if expo-av web remains broken after B1–B4.

## ESCALATION NEEDED

- **@chief-of-staff:** Confirm P0-A/B/C ordering and whether to spawn ready cards (teacher residual punch list, splash loop, Q18 QA-run) **without** unblocking the 8 HOLD parents.
- **CEO Chuck:** 15-min dogfood on teacher seat vs Super/Student; splash web at 1280 + phone portrait cold open; decide KEYGRADE “research accepted → implement later” vs park.

## RECOMMENDED NEXT ACTION

1. CoS: create **three ready** cards from P0-A/B/C (assignees: UX/fast-coder via kelyra-qa-loop for A/B; qa-engineer for C). Leave 8 HOLDs blocked.
2. ARM GRANT before any SuperGrok/Grok-Bot burn on A/B.
3. After dogfood, fold punch-list only — no TEACH-UX rewrite.
4. SchoolMarm: no class-app work; confirm Bot 8am/6:30pm CT still green offline.
