# Soft runtime = native RN (2026-09-17)

**CEO order (Chuck Broaddus):** abandon SoftMark WebView HTML host. Soft working mark is rebuilt as native React Native Views using locked orbit math from Soft Soft / Soft v8b.

## Runtime
- `src/components/ui/SoftMarkShared.tsx` — native Views: `kelyra.png` letter, eyes+glasses (no mouth), always-facing comet ball, 14-bead trail, `requestAnimationFrame` + `softCometFacing.softCometFrame` / `place(theta)`.
- `SoftMark.tsx` / `SoftMark.web.tsx` — re-export the same shared tree (no WebView / no HTML Soft host on web either).
- Locked: `COMET_ORBIT` in `softLetterScale.ts` — ovalY **0.58**, cantZDeg **14**, tilt **26**, radiusOfLetter **0.41**, trailMode **js-beads**, faceMode **eyes-glasses-nomouth**.
- Idle letter remains KelyraMark Image `kelyra.png`; SoftMark only while working.

## Design SoT (not runtime)
- `notes/company/working-k-avatar-soft-v8b.html`
- `assets/brand/soft-v8b-host.html`
- `src/components/ui/softV8bHostHtml.ts` (legacy host string; SoftMark does not import)

**WebView Soft abandoned 2026-09-17.** Do not reintroduce `react-native-webview` SoftMark or gas-orbit CSS-dash as the Soft runtime path.
